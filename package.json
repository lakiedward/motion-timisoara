{
  "name": "motion-timisoara",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace for the Playwright E2E suite in tests/. The app itself lives in motiontimisoaraApp/.",
  "license": "ISC",
  "scripts": {
    "test:e2e": "playwright test",
    "test:checkout-pricing": "playwright test --config playwright.pricing.config.ts",
    "test:live-location": "playwright test --config playwright.location.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1"
  }
}
